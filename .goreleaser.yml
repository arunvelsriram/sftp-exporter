builds:
  - goos:
      - linux
    goarch:
      - amd64
archive:
  replacements:
    linux: Linux
    amd64: x86_64
checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "{{ .Tag }}-next"
changelog:
  sort: asc
release:
  draft: true